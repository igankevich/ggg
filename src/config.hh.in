#ifndef CONFIG_HH
#define CONFIG_HH

#define MODULE_NAME @module_name@

#if defined(HIERARCHY_TEST)
#include <cstdlib>
#define HIERARCHY_ROOT std::getenv("HIERARCHY_ROOT")
#else
#define HIERARCHY_ROOT "@root_directory@"
#endif

#endif // CONFIG_HH
