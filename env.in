#!/bin/sh
set -e
prefix="@prefix@"
sharedstatedir="var"
meson configure -Dsharedstatedir="$sharedstatedir" "-Dprefix=$prefix"
mkdir -p "$prefix/lib" "$sharedstatedir"
ninja install
export GUILE_LOAD_PATH="@build_root@/src/ggg/guile${GUILE_LOAD_PATH:+:}$GUILE_LOAD_PATH"
exec "$@"
